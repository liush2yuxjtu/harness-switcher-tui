# 复制为 .env.local 后填写。绝不提交真实密钥。
# 不创建 .env.local 时，自动读取已有 ~/.config/claudex/env 的本机 CPA 配置。
CPA_BASE_URL=http://127.0.0.1:8318/v1
CPA_API_KEY=
# 两端明确使用相同模型，方便比较 harness；没有自动模型回退。
CPA_PI_MODEL=gpt-4o
CPA_CLINE_MODEL=gpt-4o
